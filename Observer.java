/*
Interface which provides functionality to observe and get updated by the subject i.e. Subject Interface on change of value
*/
public interface Observer {
	public void update(String str, boolean bool);
}